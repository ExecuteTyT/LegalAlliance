# 🔧 СРОЧНОЕ ИСПРАВЛЕНИЕ: Настройка SSL для сайта

## ❌ Проблема
- HTTP работает (код 301 - редирект)
- HTTPS не работает (код 000)
- SSL сертификат не найден

## ✅ Решение

### Шаг 1: Проверить, есть ли SSL сертификат

```bash
# Проверить наличие сертификата
ls -la /etc/letsencrypt/live/alliance-pravo.ru/

# Если папка существует, проверить файлы
ls -la /etc/letsencrypt/live/alliance-pravo.ru/*.pem
```

### Шаг 2A: Если сертификат ЕСТЬ - применить SSL конфигурацию

```bash
# Скопировать SSL конфигурацию
cp /var/www/LegalAlliance/nginx-ssl.conf /etc/nginx/sites-available/legal-alliance

# Проверить конфигурацию
nginx -t

# Если OK - перезагрузить Nginx
systemctl reload nginx

# Проверить работу
curl -I https://localhost
```

### Шаг 2B: Если сертификата НЕТ - получить новый

```bash
# 1. Установить certbot (если не установлен)
apt-get update
apt-get install -y certbot python3-certbot-nginx

# 2. Временно использовать HTTP конфигурацию (для получения сертификата)
cp /var/www/LegalAlliance/nginx.conf /etc/nginx/sites-available/legal-alliance
systemctl reload nginx

# 3. Получить SSL сертификат
certbot --nginx -d alliance-pravo.ru -d www.alliance-pravo.ru

# Certbot автоматически:
# - Получит сертификат
# - Обновит конфигурацию Nginx
# - Настроит редирект с HTTP на HTTPS

# 4. Проверить работу
curl -I https://localhost
curl -I https://alliance-pravo.ru
```

### Шаг 3: Проверить работу

```bash
# Проверить HTTP (должен редиректить на HTTPS)
curl -I http://alliance-pravo.ru

# Проверить HTTPS (должен работать)
curl -I https://alliance-pravo.ru

# Проверить статус сертификата
certbot certificates
```

## 🚀 Быстрое решение (если certbot уже установлен)

```bash
cd /var/www/LegalAlliance

# 1. Применить HTTP конфигурацию (временно)
cp nginx.conf /etc/nginx/sites-available/legal-alliance
nginx -t
systemctl reload nginx

# 2. Получить SSL сертификат
certbot --nginx -d alliance-pravo.ru -d www.alliance-pravo.ru

# Certbot автоматически настроит всё!

# 3. Проверить
curl -I https://alliance-pravo.ru
```

## 📋 Проверка после настройки

```bash
# 1. Проверить конфигурацию
nginx -t

# 2. Проверить статус Nginx
systemctl status nginx

# 3. Проверить порты
netstat -tulpn | grep -E ':(80|443)'

# 4. Проверить SSL
curl -I https://localhost
curl -I https://alliance-pravo.ru

# 5. Проверить сертификат
certbot certificates
```

## ⚠️ Если certbot не может получить сертификат

Возможные причины:
1. **Порт 80 заблокирован** - проверьте firewall
2. **Домен не резолвится** - проверьте DNS
3. **Nginx не слушает порт 80** - проверьте конфигурацию

Проверка:
```bash
# Проверить доступность порта 80 извне
curl -I http://alliance-pravo.ru

# Проверить DNS
nslookup alliance-pravo.ru

# Проверить firewall
ufw status
```

## 🔄 Альтернатива: Ручная настройка SSL

Если certbot не работает, можно настроить вручную:

```bash
# 1. Скопировать SSL конфигурацию
cp /var/www/LegalAlliance/nginx-ssl.conf /etc/nginx/sites-available/legal-alliance

# 2. Отредактировать конфигурацию (если сертификаты в другом месте)
nano /etc/nginx/sites-available/legal-alliance

# 3. Проверить и перезагрузить
nginx -t
systemctl reload nginx
```

## 📞 После настройки

После успешной настройки SSL:
- ✅ HTTP будет автоматически редиректить на HTTPS
- ✅ HTTPS будет работать с валидным сертификатом
- ✅ Сайт будет доступен по https://alliance-pravo.ru

Проверьте в браузере - сайт должен открываться по HTTPS!

