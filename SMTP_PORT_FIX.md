# Исправление SMTP порта

## Проблема
SMTP на порту 587 не работает (Connection timeout). Письма не приходят на почту.

## Решение
Изменить порт с 587 на 465 (SSL/TLS), который обычно более надежен.

## Инструкция

Выполните на сервере:

```bash
cd /var/www/LegalAlliance

# Отредактировать .env файл
nano .env
# или
vi .env
```

Найдите строку:
```
VITE_SMTP_PORT=587
```

Измените на:
```
VITE_SMTP_PORT=465
```

Сохраните файл (в nano: Ctrl+O, Enter, Ctrl+X; в vi: Esc, :wq, Enter)

Перезапустите сервер:
```bash
pm2 restart legal-alliance --update-env
```

Проверьте логи:
```bash
pm2 logs legal-alliance --lines 20
```

## Альтернатива: быстрая команда

```bash
cd /var/www/LegalAlliance
sed -i 's/VITE_SMTP_PORT=587/VITE_SMTP_PORT=465/' .env
pm2 restart legal-alliance --update-env
pm2 logs legal-alliance --lines 10
```

## Проверка

После изменения порта протестируйте форму. В логах должно появиться:
```
📧 Настройки SMTP: {
  port: 465,
  secure: true,
  ...
}
✅ Письмо отправлено на Email
```

Если порт 465 тоже не работает, попробуйте порт 25 (без шифрования):
```bash
sed -i 's/VITE_SMTP_PORT=465/VITE_SMTP_PORT=25/' .env
pm2 restart legal-alliance --update-env
```

