# 🔧 Проблема с DNS - сайт не открывается

## ❌ Проблема

Ошибка в браузере: **"Не удается найти DNS-адрес сайта alliance-pravo.ru"**

Это означает, что DNS записи домена настроены неправильно или еще не распространились.

## ✅ Решение

### 1. Проверить DNS записи домена

Нужно проверить настройки DNS у регистратора домена (где вы покупали домен).

**Требуемые DNS записи:**

```
Тип: A
Имя: @ (или alliance-pravo.ru)
Значение: 95.163.227.144
TTL: 3600 (или минимальный)

Тип: A
Имя: www
Значение: 95.163.227.144
TTL: 3600 (или минимальный)
```

### 2. Проверить текущие DNS записи

Выполните на сервере или локально:

```bash
# Проверить A-запись
nslookup alliance-pravo.ru
dig alliance-pravo.ru A

# Проверить www
nslookup www.alliance-pravo.ru
dig www.alliance-pravo.ru A

# Проверить через разные DNS серверы
dig @8.8.8.8 alliance-pravo.ru A
dig @1.1.1.1 alliance-pravo.ru A
```

**Ожидаемый результат:**
```
alliance-pravo.ru.    IN    A    95.163.227.144
```

### 3. Где настраивать DNS

Зависит от регистратора домена:

- **Reg.ru** - панель управления → DNS-серверы → Настройка DNS
- **Timeweb** - панель управления → Домены → DNS-записи
- **REG.RU / Другие** - панель управления доменом → DNS-зона

### 4. Проверить, что сервер доступен по IP

```bash
# На сервере
curl -I http://localhost:3001/api/health

# Извне (должен работать)
curl -I http://95.163.227.144
```

### 5. Временное решение (для тестирования)

Можно временно добавить в файл `hosts` на вашем компьютере:

**Windows:** `C:\Windows\System32\drivers\etc\hosts`
**Linux/Mac:** `/etc/hosts`

Добавить строку:
```
95.163.227.144    alliance-pravo.ru
95.163.227.144    www.alliance-pravo.ru
```

⚠️ Это только для тестирования! Для всех пользователей нужно настроить DNS правильно.

## 🔍 Диагностика DNS

### Проверка с сервера

```bash
# На сервере выполните
nslookup alliance-pravo.ru
host alliance-pravo.ru
dig alliance-pravo.ru +short
```

### Проверка онлайн

Используйте онлайн-инструменты:
- https://dnschecker.org/#A/alliance-pravo.ru
- https://www.whatsmydns.net/#A/alliance-pravo.ru

Введите домен и проверьте, что везде показывается IP: `95.163.227.144`

## ⏱️ Время распространения DNS

После изменения DNS записей:
- **Обычно:** 15 минут - 2 часа
- **Максимум:** до 48 часов (редко)
- **TTL влияет:** чем меньше TTL, тем быстрее обновление

## 📋 Чек-лист

- [ ] Проверены DNS записи у регистратора
- [ ] A-запись для `@` указывает на `95.163.227.144`
- [ ] A-запись для `www` указывает на `95.163.227.144`
- [ ] Проверено через `nslookup` или `dig`
- [ ] Проверено через онлайн-инструменты
- [ ] Прошло достаточно времени для распространения DNS

## 🚨 Если DNS настроены правильно, но сайт не работает

1. **Проверить firewall на сервере:**
   ```bash
   ufw status
   # Должны быть открыты порты 80 и 443
   ```

2. **Проверить Nginx:**
   ```bash
   systemctl status nginx
   nginx -t
   ```

3. **Проверить, что сервер отвечает:**
   ```bash
   curl -I http://95.163.227.144
   curl -I https://95.163.227.144
   ```

4. **Проверить логи Nginx:**
   ```bash
   tail -f /var/log/nginx/legal-alliance-error.log
   ```

## 📞 Контакты для настройки DNS

Если не знаете, где настраивать DNS:
1. Найдите, у кого зарегистрирован домен (проверьте whois)
2. Зайдите в панель управления регистратора
3. Найдите раздел "DNS" или "DNS-зона"
4. Добавьте/измените A-записи как указано выше

