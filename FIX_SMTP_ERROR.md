# 🔧 Исправление ошибки SMTP

## Проблема

В логах обнаружена ошибка:
```
ReferenceError: smtpPort is not defined
at file:///var/www/LegalAlliance/server.js:138:36
```

Также есть проблемы с SMTP таймаутами при подключении к `mail.alliance-pravo.ru`.

## ✅ Решение

Исправлен код в `server.js` для более надежной обработки ошибок SMTP.

## 📋 Что нужно сделать на сервере

### 1. Обновить код на сервере

```bash
# Перейти в директорию проекта
cd /var/www/LegalAlliance

# Обновить код из репозитория (если используете git)
git pull

# Или скопировать исправленный server.js вручную
```

### 2. Перезапустить приложение

```bash
# Перезапустить через PM2
pm2 restart legal-alliance

# Проверить статус
pm2 status

# Проверить логи
pm2 logs legal-alliance --lines 20
```

### 3. Проверить работу

```bash
# Проверить health endpoint
curl http://localhost:3001/api/health

# Должно вернуть:
# {"status":"ok","telegram":true,"smtp":true}
```

## 🔍 Дополнительная диагностика SMTP

Если проблемы с SMTP продолжаются, проверьте:

### 1. Доступность SMTP сервера

```bash
# Проверить доступность порта 587
telnet mail.alliance-pravo.ru 587

# Или
nc -zv mail.alliance-pravo.ru 587

# Проверить порт 465
nc -zv mail.alliance-pravo.ru 465
```

### 2. Проверить настройки в .env

```bash
cat /var/www/LegalAlliance/.env | grep SMTP
```

Должны быть:
- `VITE_SMTP_HOST=mail.alliance-pravo.ru`
- `VITE_SMTP_PORT=587` (или 465)
- `VITE_SMTP_USER=info@alliance-pravo.ru`
- `VITE_SMTP_PASSWORD=...`

### 3. Протестировать SMTP подключение

```bash
cd /var/www/LegalAlliance
node test-smtp.js
```

## 📝 Важные замечания

1. **Telegram работает** - заявки отправляются в Telegram, даже если Email не работает
2. **Сайт работает** - основная функциональность не нарушена
3. **SMTP таймауты** - это проблема подключения к почтовому серверу, не критично для работы сайта

## 🚨 Если ошибка повторяется

1. Проверьте логи: `pm2 logs legal-alliance --err --lines 50`
2. Убедитесь, что код обновлен: `cat server.js | grep -A 5 "alternativePorts"`
3. Проверьте версию Node.js: `node --version` (должна быть 18+)

## ✅ После исправления

После применения исправлений ошибка `ReferenceError: smtpPort is not defined` больше не должна появляться в логах.

SMTP таймауты могут продолжаться, если есть проблемы с почтовым сервером, но это не будет вызывать падение приложения.

